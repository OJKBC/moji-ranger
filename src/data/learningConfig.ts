/**
 * 出題バランスの設定値。ここの数値を変えるだけで調整できる（ハードコードしない）。
 */
export const LEARNING = {
  /**
   * 復習（苦手・間隔反復由来）の出題割合の上限。
   * 残りは解放済みプール全体から「まんべんなく」選ぶ。
   */
  maxReviewRatio: 0.3,
  /** 直近この問数に出た文字は選ばれにくくする（同じ文字の張り付き防止） */
  recentWindow: 3,
  /** 直近に出た文字への減点（大きいほど避ける） */
  recentPenalty: 8,
  /** まだ一度も出題していない文字への加点（必ず早めに露出させる） */
  unseenBoost: 5,
  /** 出題回数1回あたりの減点（出題回数が少ない文字を優先し、全文字を露出させる） */
  seenPenaltyPerCount: 0.6,
} as const
